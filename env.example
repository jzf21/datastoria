# ClickHouse Console Environment Variables
# Copy this file to .env.local and fill in your API keys

# ============================================
# AI Chat Feature (Required for chat)
# ============================================

# Anthropic API Key (fallback - Claude support)
# Get your API key from: https://console.anthropic.com/
# ANTHROPIC_API_KEY=sk-ant-...

# OpenAI API Key (highest priority - GPT-4o support)
# Get your API key from: https://platform.openai.com/api-keys
# OPENAI_API_KEY=sk-...

# Google Generative AI API Key (medium priority - Gemini-2.5 support)
# Get your API key from: https://aistudio.google.com/app/apikey
# GOOGLE_GENERATIVE_AI_API_KEY=...

# Mock Mode (for development/testing - avoids API costs)
# Set to "true" to use mock LLM responses instead of real API calls
# USE_MOCK_LLM=false

# Optional: Rate limiting (requests per minute)
# RATE_LIMIT_PER_MINUTE=10

# ============================================
# Connection Provider (Optional)
# ============================================

# Enable connection provider feature (optional)
# Set to "true" to enable fetching connection templates from an endpoint
# NEXT_PUBLIC_CONSOLE_CONNECTION_PROVIDER_ENABLED=false

# Connection provider endpoints (only needed if provider is enabled)
# NEXT_PUBLIC_CONSOLE_CONNECTION_PROVIDER_ENDPOINT_DEV=http://localhost:8080/connections
# NEXT_PUBLIC_CONSOLE_CONNECTION_PROVIDER_ENDPOINT_PRD=https://api.example.com/connections

